node_modules
dist
dist-ssr
.DS_Store
*.local
.vite

# Generated by scripts/fetch-vision-assets.mjs — never commit (28MB of binaries)
public/vision/

# Dev-only video simulation harness scratch
tools/video-sim/.tmp/
tools/video-sim/out/
tools/video-sim/node_modules/

# NOTE: public/demo/ IS committed. The demo clip is deliberately published —
# it ships with the app so windscreen mode can be tried without a camera.
