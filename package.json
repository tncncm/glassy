{
  "name": "glassy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run vision-assets && vite",
    "build": "npm run vision-assets && tsc --noEmit && vite build",
    "vision-assets": "node scripts/fetch-vision-assets.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "generate-icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1",
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.0.7",
    "vite-plugin-pwa": "^0.21.1"
  }
}
